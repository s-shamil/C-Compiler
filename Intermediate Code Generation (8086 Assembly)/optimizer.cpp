#include <bits/stdc++.h>
using namespace std;

#define rep(i,n) for(auto i=0; i<(n); i++)
#define mem(x,val) memset((x),(val),sizeof(x));
#define write(x) freopen(x,"w",stdout);
#define read(x) freopen(x,"r",stdin);
#define sqr(x) ((x)*(x))
#define pb push_back
#define clr clear()
#define inf (1<<30)
#define eps 1e-9
#define PI acos(-1)
#define bug printf("hello\n");

typedef long long ll;
typedef unsigned long long ull;
typedef pair<int,int> pii;

ifstream long_code;
ofstream short_code;
vector<string> lines;
int main() {
    //read("test.txt");
    long_code.open("code.asm");
    short_code.open("optimized_code.asm");
    string s;
    while(getline(long_code, s)){
        istringstream iss(line);

    }

    return 0;
}

